package com.deepseat.ds.api

class APIUrls {
    companion object {
        val HOSTNAME = "soc06212.iptime.org:8080"

        // User
        val API_REGISTER  = HOSTNAME + "/register"
        val API_LOGIN  = HOSTNAME + "/login"
        val API_LOGOUT  = HOSTNAME + "/logout"
        val API_USER  = HOSTNAME + "/user"

        // Document

    }
}